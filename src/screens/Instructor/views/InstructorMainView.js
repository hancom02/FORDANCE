import { Text, View } from "react-native";

const InstructorMainView = (props) => {
    const {
        navigation,
        user
    } = props;
    return(
        <View>
            <Text>Instructor Main View</Text>
        </View>
    )
}

export default InstructorMainView;