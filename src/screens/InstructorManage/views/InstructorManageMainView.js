const { View, Text } = require("react-native")

const InstructorManageMainView = (props) => {
    const {
        navigation
    }  = props;

    return (
        <View>
            <Text>Manage Class and Program of Instructor</Text>
        </View>
    )
}

export default InstructorManageMainView;