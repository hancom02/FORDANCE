import { Text, View } from "react-native";

const ScheduleMainView = (props) => {
    const {
        navigation,
        
    } = props;
    return(
        <View>
            <Text>Schedule Main View</Text>
        </View>
    )
}

export default ScheduleMainView;